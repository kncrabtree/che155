# Legacy environment files

These files are retained for historical reference only and are **not** used by
the current course.

- `che155-old.yml` — single-environment definition from the original course
  (Lee-Ping Wang's version). Superseded by the four split environments in
  `assets/text/` (`che155-core.yml`, `che155-quantum.yml`,
  `che155-dynamics.yml`, `che155-ml.yml`).
- `environment.yml` — byte-identical duplicate of `che155-old.yml`, kept here
  in case any older link still references it.

Do not link to these files from `installation.md` or course pages.
